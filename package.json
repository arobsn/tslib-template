{
  "name": "ts-lib-template",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "check": "biome check",
    "fix": "biome check --write",
    "test": "bun test",
    "watch:test": "bun test --watch"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
